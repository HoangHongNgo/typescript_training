/**
 * Types of SnackBar
 */
export enum SnackbarType {
  Warning = 'warning',
  Success = 'success',
}

/**
 * Types of validator result
 */
export enum ValidatorResultType {
  Valid,
  Missing,
  Invalid,
  Existing,
}
